name: Release

on:
  push:
    tags:
      - 'v*-open'

jobs:
  build-lite:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install NSIS
        run: |
          choco install nsis -y
          echo "C:\Program Files (x86)\NSIS" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1
      - name: Fetch bind
        run: |
          New-Item -ItemType Directory -Force -Path shared | Out-Null
          aws s3 cp s3://ikuku-releases/bind/bind.tar.gz shared/bind.tar.gz
      - name: Fetch tray app binary
        run: aws s3 cp s3://ikuku-releases/tray/ikuku-tray.exe ikuku-tray.exe
      - name: Build lite
        run: powershell -ExecutionPolicy Bypass -File build-installer.ps1 -Variant lite
      - uses: actions/upload-artifact@v4
        with:
          name: ikuku-open-lite
          path: ikuku-open-lite.exe

  build-full:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install NSIS
        run: |
          choco install nsis -y
          echo "C:\Program Files (x86)\NSIS" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1
      - name: Fetch bundle + bind
        run: |
          New-Item -ItemType Directory -Force -Path shared | Out-Null
          aws s3 sync s3://ikuku-releases/bundle/ bundle/
          aws s3 cp s3://ikuku-releases/bind/bind.tar.gz shared/bind.tar.gz
      - name: Fetch tray app binary
        run: aws s3 cp s3://ikuku-releases/tray/ikuku-tray.exe ikuku-tray.exe
      - name: Build full
        run: powershell -ExecutionPolicy Bypass -File build-installer.ps1 -Variant full
      - name: Zip full package
        run: Compress-Archive -Path ikuku-open-full.exe, bundle -DestinationPath ikuku-open-full.zip
      - uses: actions/upload-artifact@v4
        with:
          name: ikuku-open-full
          path: ikuku-open-full.zip

  release:
    needs: [build-lite, build-full]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: ikuku-open-lite
      - uses: actions/download-artifact@v4
        with:
          name: ikuku-open-full
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ikuku-open-lite.exe
            ikuku-open-full.zip
          generate_release_notes: true
